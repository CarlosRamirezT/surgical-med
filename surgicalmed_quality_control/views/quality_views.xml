<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="quality_check_view_form" model="ir.ui.view">
        <field name="name">quality.check.view.form</field>
        <field name="model">quality.check</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="do_pass" type="object" class="btn-primary"
                        invisible="quality_state != 'none'"
                        string="Pass" data-hotkey="q" />
                    <button name="do_pass" type="object" invisible="quality_state != 'fail'"
                        groups="quality.group_quality_manager" string="Pass" data-hotkey="q" />
                    <button name="do_fail" type="object" class="btn-primary"
                        invisible="quality_state != 'none'"
                        string="Fail" data-hotkey="w" />
                    <button name="do_fail" type="object" invisible="quality_state != 'pass'"
                        groups="quality.group_quality_manager" string="Fail" data-hotkey="w" />
                    <button name="do_measure" type="object"
                        invisible="test_type != 'measure' or quality_state != 'none'"
                        string="Measure"
                        data-hotkey="v" />
                    <button name="do_alert" type="object"
                        invisible="alert_ids or quality_state != 'pass'"
                        string="Create Alert" data-hotkey="g" />
                    <button name="do_alert" type="object" class="btn-primary"
                        invisible="alert_ids or quality_state != 'fail'" string="Create Alert"
                        data-hotkey="g" />
                    <field name="quality_state" widget="statusbar" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_see_alerts" icon="fa-bell" type="object"
                            class="oe_stat_button" invisible="alert_count == 0">
                            <field name="alert_count" string="Alerts" widget="statinfo" />
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

</odoo>